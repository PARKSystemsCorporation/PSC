# Force MSVC toolset for node-gyp builds. Node 24 sets clang=1 by default,
# which forces the ClangCL MSBuild toolset — only v143 (MSVC) is installed
# here, so we override to fall back. Remove this if you install the
# "MSBuild support for LLVM (clang-cl)" component in Visual Studio.
clang=false
